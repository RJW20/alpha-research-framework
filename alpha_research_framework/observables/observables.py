from alpha_research_framework.observables.observable import Observable


class Open(Observable):
    NAME = "adj_open"

class High(Observable):
    NAME = "adj_high"

class Low(Observable):
    NAME = "adj_low"

class Close(Observable):
    NAME = "adj_close"

class Volume(Observable):
    NAME = "volume"
